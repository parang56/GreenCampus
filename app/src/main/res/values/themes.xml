<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CarrotChegg" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="warning_text">
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">#99000000</item>
    </style>

    <style name="Theme.CarrotChegg" parent="Base.Theme.CarrotChegg" />

    <style name="Theme.AuthWithGoogle" parent="Base.Theme.CarrotChegg"/>
</resources>